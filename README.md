# Membase user guide

Published with GitBook. This repository is a **mirror**: the source of truth is
`docs/user-guide/` in `unibaseio/membase-suites`, and `scripts/sync-user-guide.sh` there
pushes every change here. Do not edit files in this repository directly — the next sync
overwrites them.
